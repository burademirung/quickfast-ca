import { z, defineCollection } from 'astro:content';

// All content collections removed — AstroWind's sample post collection was deleted.
// If we need static content collections later, define them here.
export const collections = {};
